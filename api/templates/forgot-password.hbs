<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Reset Your Password - EchoSwap</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        margin: 0;
        padding: 0;
        background-color: #f7f9fc;
        font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        color: #333;
      }

      .email-container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
      }

      .logo {
        display: block;
        margin: 0 auto 25px;
        max-width: 140px;
      }

      h1 {
        font-size: 24px;
        color: #0d6efd;
        margin-bottom: 10px;
        text-align: center;
      }

      p {
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 20px;
      }

      .highlight-box {
        background-color: #f0f4ff;
        border-left: 4px solid #0d6efd;
        padding: 15px 20px;
        margin-bottom: 30px;
      margin-top:30px;
        border-radius: 6px;
      }

      .btn {
        display: inline-block;
        background-color: #0d6efd;
        color: #ffffff !important;
        padding: 14px 24px;
        text-decoration: none;
        border-radius: 6px;
        font-weight: bold;
        text-align: center;
        margin-top: 10px;
      }

      .footer {
        font-size: 13px;
        color: #999;
        text-align: center;
        margin-top: 40px;
      }

      @media (max-width: 600px) {
        .email-container {
          padding: 20px;
        }

        h1 {
          font-size: 20px;
        }

        .btn {
          display: block;
          width: 100%;
          text-align: center;
        }
      }
    </style>
  </head>

  <body>
    <div class="email-container">
      <img src="https://i.ibb.co/ynyPgj84/website-logo.png" alt="EchoSwap Logo" class="logo" />
      <h1>Reset Your Password</h1>

      <p>Hello <strong>{{name}}</strong>,</p>

      <p>We received a request to reset your password for your EchoSwap account. If this was you, you can reset your password using the button below.</p>

      <a href="{{resetLink}}" class="btn">Reset Password</a>

      <div class="highlight-box">
        <p><strong>Name:</strong> {{name}}<br />
        <strong>Email:</strong> {{email}}<br />
        <strong>Request Time:</strong> {{requestTime}}</p>
      </div>

      <p>If you didn't request a password reset, you can safely ignore this email. This link will expire in <strong>{{expireMinutes}} minutes</strong>.</p>

      <div class="footer">
        &copy; {{year}} EchoSwap. All rights reserved.<br />
        Need help? <a href="{{supportUrl}}">Contact Support</a>
      </div>
    </div>
  </body>
</html>
